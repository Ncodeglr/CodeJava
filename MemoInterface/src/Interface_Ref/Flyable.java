//Une interface est une class abstraite dont toutes les méthodes sont abstraites
//On peut oter le abstract 

//Quand une class implemente une interface, elle doit récrire l'ensemble des méthode que possède l'interface

package Interface_Ref;

 interface Flyable {
	 public void fly(String energy);

}
